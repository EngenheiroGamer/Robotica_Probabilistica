% -- FORMATAÇÃO DE TEXTO --

% Duplo Salto de Linha:
%\def \dblnewline {\\[1.5ex]}
\def \duploEnter {\par\vspace{1.5em}\par}

% Travessão:
\def \travessao {---}

% Hífen:
\def \hifen {-}

% Acento Til:
\def \til {\text{$\sim$}}

% Acento Til com espaçamento:
\def \tilEspacado {\; \text{$\sim$} \;}

% Quebra de Linha:
\def \br {\newline}

% Espaço Fino:
\def \espacoFino {\,}

% Espaço Largo:
\def \espacoLargo {\;}

% Texto em Negrito:
\def \negrito#1{\textbf{#1}}

% Texto em Itálico:
\def \italico#1{\textit{#1}}

% Texto Sublinhado:
\def \sublinhado#1{\underline{#1}}

% Texto sobrescrito:
\def \textoSobrescrito#1#2{$#1 ^ {#2}$}

% Texto subscrito:
\def \textoSubscrito#1#2{$#1 _ {#2}$}

% Texto subscrito e sobrescrito:
\def \textoSubsobrescrito#1#2#3{$#1 _ {#2}^{#3}$}

% Texto entre aspas duplas
\def \aspas#1{``#1''}

% -- UNIDADES --

% TENSÃO:

% V:
\def \V {\; \text{V}}

% mV:
\def \mV {\; \text{mV}}

% MV:
\def \MV {\; \text{MV}}

% uV:
\def \uV {\; \mu\text{V}}

% CORRENTE:

% A:
\def \A {\; \text{A}}

% mA:
\def \mA {\; \text{mA}}

% uA:
\def \uA {\; \mu\text{A}}

% nA:
\def \nA {\; \text{nA}}

% RESISTÊNCIA: 

% Ω:
\def \Ohm {\; \Omega}

% mΩ:
\def \mOhm {\; \text{m}\Omega}

% uΩ:
\def \uOhm {\; \mu\Omega}

% nΩ:
\def \nOhm {\; \text{n}\Omega}

% POTÊNCIA:

% W:
\def \W {\; \text{W}}

% mW:
\def \mW {\; \text{mW}}

% uW:
\def \uW {\; \mu\text{W}}

% nW:
\def \nW {\; \text{nW}}

% kW:
\def \kW {\; \text{kW}}

% MW:
\def \MW {\; \text{MW}}

% INDUTÂNCIA:

% H:
\def \H {\; \text{H}}

% mH:
\def \mH {\; \text{mH}}

% uH:
\def \uH {\; \mu\text{H}}

% nH:
\def \nH {\; \text{nH}}

% CAPACITÂNCIA:

% F:
\def \F {\; \text{F}}

% mF:
\def \mF {\; \text{mF}}

% uF:
\def \uF {\; \mu\text{F}}

% nF:
\def \nF {\; \text{nF}}

% pF:
\def \pF {\; \text{pF}}

% FREQUÊNCIA:

% Hz:
\def \Hz {\; \text{Hz}}

% kHz:
\def \kHz {\; \text{kHz}}

% MHz:
\def \MHz {\; \text{MHz}}

% GHz:
\def \GHz {\; \text{GHz}}

% mHz:
\def \mHz {\; \text{mHz}}

% uHz:
\def \uHz {\; \mu\text{Hz}}

% nHz:
\def \nHz {\; \text{nHz}}

% TEMPO:

% s:
\def \s {\; \text{s}}

% ms:
\def \ms {\; \text{ms}}

% us:
\def \us {\; \mu\text{s}}

% ns:
\def \ns {\; \text{ns}}

% ps:
\def \ps {\; \text{ps}}

% DISTÂNCIA:

% m:
\def \m {\; \text{m}}

% cm:
\def \cm {\; \text{cm}}

% mm:
\def \mm {\; \text{mm}}

% um:
\def \um {\; \mu\text{m}}

% nm:
\def \nm {\; \text{nm}}

% MASSA:

% kg:
\def \kg {\; \text{kg}}

% g:
\def \g {\; \text{g}}

% mg:
\def \mg {\; \text{mg}}

% ug:
\def \ug {\; \mu\text{g}}

% ng:
\def \ng {\; \text{ng}}

% VOLUME:

% L:
\def \L {\; \text{L}}

% mL:
\def \mL {\; \text{mL}}

% uL:
\def \uL {\; \mu\text{L}}

% nL:
\def \nL {\; \text{nL}}

% pL:
\def \pL {\; \text{pL}}

% m³:
\def \m3 {\; \text{\textoSobrescrito{m}{3}}}